import { SettingsScreen } from '../../../src/screens/SettingsScreen';

/**
 * Route that renders the settings hub inside the tabs stack.
 */
export default function SettingsRoute() {
  return <SettingsScreen />;
}
