import { WorkingGroupsScreen } from '../../src/screens/WorkingGroupsScreen';

/**
 * Route that renders the working groups screen inside the tabs stack.
 */
export default function WorkingGroupsRoute() {
  return <WorkingGroupsScreen />;
}
