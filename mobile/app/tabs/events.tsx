import { EventsScreen } from '../../src/screens/EventsScreen';

/**
 * Route that renders the events screen inside the tabs stack.
 */
export default function EventsRoute() {
  return <EventsScreen />;
}
