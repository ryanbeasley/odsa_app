import { SupportDetailsScreen } from '../../src/screens/SupportDetailsScreen';

/**
 * Route that renders the support/details screen inside the tabs stack.
 */
export default function SupportRoute() {
  return <SupportDetailsScreen />;
}
